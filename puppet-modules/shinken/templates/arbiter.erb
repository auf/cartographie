define arbiter{
  arbiter_name  <%= name %>
  address       <%= address %>
  host_name     <%= scope.lookupvar("::hostname") %>
  port          <%= port %>
  spare         <%= spare %>
  <% if modules then -%>modules       <%= modules %><% end %>
}
